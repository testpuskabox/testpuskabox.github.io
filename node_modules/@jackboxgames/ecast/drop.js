class DropEntity {
    constructor(options) {
        this.key = options.key;
    }

    whenReceived(client) {
        delete client.entities[this.key];
    }

    toString() {
        return `DropEntity{\n\tkey:${this.key}\n}`;
    }
}

module.exports = {
    DropEntity
};
