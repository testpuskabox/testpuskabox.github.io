const { APIClient } = require('./api');
const { WSClient } = require('./ws');
const { CreateRoomReply, GetRoomReply } = require('./room');
const { ClientWelcome, ClientDisconnected } = require('./client');
const { ArtifactEntity } = require('./artifact');
const { GCounter } = require('./g_counter');
const { NumberEntity } = require('./number');
const { TextEntity } = require('./text');
const { DoodleEntity } = require('./doodle');
const { ObjectEntity } = require('./object');
const { CountGroup } = require('./count_group');
const { DropEntity } = require('./drop');
const { OK } = require('./ok');
const { RoomExit } = require('./room');
const { TextRing } = require('./text_ring');
const { TextMapEntity } = require('./text_map');
const { PNCounter } = require('./pn_counter');

module.exports = {
    // Clients
    APIClient,
    WSClient,
    // Opcodes
    ClientWelcome,
    CreateRoomReply,
    DropEntity,
    GetRoomReply,
    ClientDisconnected,
    RoomExit,
    OK,
    // Entities
    ArtifactEntity,
    DoodleEntity,
    NumberEntity,
    CountGroup,
    GCounter,
    ObjectEntity,
    PNCounter,
    TextEntity,
    TextRing,
    TextMapEntity
};
